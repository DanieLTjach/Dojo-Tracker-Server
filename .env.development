# Development Environment Configuration
NODE_ENV=development
PORT=3000
DB_PATH=./db/data/data.db

# JWT Configuration (development - use strong secret in production)
JWT_SECRET=dev_jwt_secret_change_this_in_production_to_random_string
JWT_EXPIRY=7d

# Authentication settings (1 hour validity)
AUTH_INIT_DATA_VALIDITY_SECONDS=3600

FRONTEND_URL=http://localhost:3000

# Telegram
BOT_TOKEN=dev_bot_token_replace_with_real