node_modules
.vscode
db/data
coverage

# macOS files
.DS_Store

# Environment files: ignore all except the two tracked ones
.env*
!.env.example
!.env.test

# ZCode session artifacts (plans etc.)
.zcode/
notes/

# Firebase service account keys (used by storage:cleanup) - secrets, never commit
service-account*.json
*-firebase-adminsdk-*.json
